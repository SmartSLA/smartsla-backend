.modal.center(tabindex="-1", role="dialog", ng-controller="ticGroupDeleteModalController as ctrl")
  .modal-dialog
    .modal-content
      .modal-header
        button.close(type="button", ng-click="$toggle()")
        h4.modal-title {{ 'Delete a group' | translate }}
      .modal-body
        | {{ 'Are you sure you want to delete this group ?' | translate }}
        .tic-group-delete-modal.tic-group-name
          | {{ group.name }}
      .modal-footer
        button.btn.btn-default(type="button", ng-click="ctrl.deleteGroup(group); $toggle()") {{ 'Confirm' | translate }}
        button.btn.btn-default(type="button", ng-click="$toggle()") {{ 'Cancel' | translate }}
